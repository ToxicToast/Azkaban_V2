# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build gateway-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t gateway-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3012

WORKDIR /app

RUN addgroup --system twitch-hook-service && \
          adduser --system -G twitch-hook-service twitch-hook-service

COPY dist/apps/twitch-hook-service .
RUN chown -R twitch-hook-service:twitch-hook-service .
RUN npm install

CMD [ "node", "main.js" ]
